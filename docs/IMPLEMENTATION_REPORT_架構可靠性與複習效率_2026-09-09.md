# 架構可靠性與複習效率執行報告

對應交接書：[`HANDOFF_架構可靠性與複習效率_2026-09-07.md`](HANDOFF_架構可靠性與複習效率_2026-09-07.md)

## 批次狀態

| 批次 | 狀態 | 主要證據 |
| --- | --- | --- |
| A 答案防洩漏 | 已驗證 | 完整解答、雙解矩陣與稽核資訊都位於第④段之後；測試覆蓋。 |
| B 單一提交交易 | Sol 已批准 | attempt journal、回復、冪等、PE/GK 路由與失敗反例。 |
| C 回想升級語意 | Sol 已批准 | 每級兩次、升級 streak 歸零、舊資料保守遷移。 |
| D 到期日期與佇列 | Sol 已批准 | 本地日曆日、月底／年底、評分與略過分流、單一 queue owner。 |
| E 共用閱讀體驗 | Sol 已批准程式行為；視覺未驗 | 逐 QID 保存 modal、左右捲動、小題、揭露段數與手機窗格；備份往返、焦點圈限。Sol 複核保存層級還原及焦點拉回行為。 |
| F 多樣性抽題 | Sol 已批准 | 正式路徑以穩定 chapter ID 與白名單 formulaTags 映射抽題；321 題中 60 題有可追溯題型、261 題保守維持 unknown。跨章節、同章節異型、7 天與不足候選皆有測試。 |
| G 下一步與摘要 | Sol 已批准 | 原題一鍵蓋牌、逐題自評／錯因摘要、明確且冪等的到期加入、統計語意拆分、人類題名；本輪完成數不再混入歷史完成題數。 |
| H 發布與效能 | Sol 已批准離線閘門；瀏覽器時間未驗 | CI 上傳 `_site`、映射資產缺漏即失敗、靜態體積基線。本機 HTTP 預覽發現並修正四個執行期函式庫漏打包；以暫存 fixture、逐檔缺失與不安全路徑回歸測試鎖定。 |

## 最新自動驗收

- 2026-09-11 每日練習 UX 修正：底部新增「完成本題並進入下一題」；最後一題顯示「完成本題並查看本輪摘要」；「暫存本題進度」保留。按鈕只開啟既有四段蓋牌與自評流程，不直接完成或前進。
- 2026-09-11 詳解前台精簡：一般詳解與四段蓋牌不再渲染維護者稽核卡、回報文字、來源路徑與內部識別欄位；官方原題裁切圖／PDF、題目、學習狀態、詳解內容與內部稽核 metadata／測試仍保留。
- `scripts/run_all_tests.py`：303 項通過。
- `scripts/check_html_js_syntax.py`：inline JavaScript 通過。
- `scripts/verify_slicing_and_links.py`：PE 321／321、GK 161／161；切片及本地 PDF 錯誤皆為 0，外部 URL 僅驗格式、不宣稱連通。
- `scripts/stage_pages_artifact.py`：1,074 個檔案、約 190.6 MiB，所有資料庫 PDF／crop／image-map 目標及 KaTeX／Marked 執行期檔案存在。
- 本機 HTTP 資產探針：首頁、`katex.min.css`、`katex.min.js`、`auto-render.min.js`、`marked.min.js` 均回應 200。
- `scripts/health_check_codebase.py`：80／100；唯一扣分為刻意保留的 2 題人工檢核，不是程式或發布閘門失敗。
- `git diff --check`：通過。

## 未驗與待使用者判定

- 自動瀏覽器安全政策禁止開啟本機 `file://`，因此 360／390／768／1280、亮暗色、慢圖片、實際 Tab 圈限及冷／暖快取時間仍未宣稱通過。
- 真人手機操作手感與「三題、四段、7 天」教學參數效果，需要候選版試用資料，不能由單元測試推定。
- PE 仍有 2 題 `needs_manual_review`；本次未為提高健康分數而改動題解狀態。

## 回復方式

本輪變更已通過自動驗收與 Sol 複審，將以單一版本提交並發布。若候選版人工驗收不通過，可直接針對本輪差異修正；不得使用會覆蓋使用者既有工作樹的 destructive reset。
