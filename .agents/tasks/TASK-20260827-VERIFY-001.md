# 📋 Task Packet

## Metadata
- **Task ID**: `TASK-20260827-VERIFY-001`
- **Sender Runtime**: `Codex`
- **Target Runtime**: `Antigravity`
- **Hop Count**: `1`
- **Target Branch**: `main`

## Objective

依據 Codex 的核心架構意見，檢查並改善技師考試知識庫的驗證流程，優先降低題目、題解、數學推導與介面資料不同步造成的錯誤。

## Context & Scope

Codex 負責核心資料模型、數學驗證器、測試閘門與整合決策。Antigravity/Gemini 負責瀏覽器檢查、UI 呈現與依本封包執行的局部修正。

Relevant files:

- `PROJECT.yaml`
- `.agents/rules/collaboration_contract.md`
- `.agents/rules/exam_solution_standards.md`
- `dashboard-data.js`
- `solutions-bundle.js`
- `src/tools/`、`scripts/tools/`
- `tests/`
- `index.html`

## Required Work

1. 只讀檢查目前題目卡片、題解視窗、驗證徽章與錯題／複習狀態的實際行為。
2. 找出 UI 顯示與資料來源不一致的項目，尤其是題目 ID、科目、年度、題號、題解連結與複習狀態。
3. 依現有資料結構提出最小修改；不得自行重寫核心資料庫或改變題目 ID。
4. 對 UI 或瀏覽器可驗證的問題進行局部修正。
5. 執行瀏覽器檢查與相關本地測試，回報失敗證據，不得以「畫面看起來正常」取代測試。

## Acceptance Criteria

- [ ] 沒有新增或刪除題目資料。
- [ ] 沒有改變既有題目 ID、年份、科目與題號對應。
- [ ] 題目卡片與題解視窗使用同一個題目 ID。
- [ ] 複習狀態不會因重新載入頁面而錯配到其他題目。
- [ ] UI 修正已通過相關測試與瀏覽器驗證。
- [ ] 產生完整 Result Packet，包含狀態、變更、證據、測試結果、風險與未解決事項。

## Non-Goals / Constraints

- 不得修改核心數學驗證器、SM-2 演算法、題目資料庫格式或題解內容；這些由 Codex 負責。
- 不得直接修改自動產生的資料庫 `.js`；如需修改，回報產生來源。
- 不得自動再轉交給其他外部 Runtime。
- 不得同時修改 Codex 正在處理的核心檔案。
- 發現題解數值或推導疑似錯誤時，只建立問題清單與證據，不自行宣告答案正確。

## Stop Condition

若需要變更資料模型、題目 ID、數學驗證器或自動產生流程，停止修改並在 Result Packet 中列為 `UNRESOLVED`，交回 Codex 決策。

## Return Target

將成果寫入 `.agents/results/TASK-20260827-VERIFY-001.md`，或直接回報同等格式的 Result Packet。
