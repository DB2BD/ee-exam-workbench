<!--
Each task description MUST state:
- the behavior or contract being delivered (what is observably true when the
  task is complete), and
- the verification target that proves completion (test, CLI invocation,
  analyzer check, manual assertion, or content review).

File paths are supporting context for locating the work, never the task
itself. "Edit file X" is not a valid task — it is missing both behavior and
verification.
-->

## 1. 正式教材定位與內容標準

- [x] 1.1 更新 `docs/產品化企劃書_2026-09-05.md`，使產品定位、核心體驗、題解格式、題目編排與非目標一致承諾高效刷題及可得分的精確解答；以逐節內容審查確認包含「考場標準作答、得分點拆解、完整教學推導、獨立驗算與常見失分」及「母題 → 同型題 → 變式題 → 混合／整卷」，並確認未把 AI 聊天、社群、排行榜或遊戲化列為近期範圍。

## 2. 出版品質與執行路線

- [x] 2.1 [after: 1.1] 補齊同一企劃文件的內容品質閘門、人工覆核與跨文件數字治理、小規模考生試用指標及分階段優先序，使正式出版前的阻擋條件和完成證據可被下一位執行者直接判定；以內容審查確認所有數量都有現況來源或明示待盤點、驗收指標不以功能數或自動測試數替代學習成效，並以 `spectra analyze formal-textbook-product-proposal --json` 與 `spectra validate formal-textbook-product-proposal` 無 Critical／validation failure 作為完成證據。

## 3. 上榜優先範圍收斂

變更紀錄：2026-09-21 使用者以「目前首要目標是先考上」取代本 change 的正式教材優先順序。任務 1.1 與 2.1 保留為已完成歷史；下列補償任務只調整本次執行範圍，不宣稱已完成個人能力診斷或題庫改寫。

- [x] 3.1 [after: 2.1] 更新 `docs/產品化企劃書_2026-09-05.md`，使本次成功條件只包含「六科核心題與個人弱點基線、核心題得分型解答、母題式練習路徑、錯因複習與計時模考」四項，並把正式出版、單一發布清單、外部考生試用、商業化及非核心人工覆核移至後續階段；以逐節內容審查確認文件明示沒有個人作答資料前不推定弱點、7 題人工覆核只在影響核心備考時優先處理，並以 `spectra analyze formal-textbook-product-proposal --json`、`spectra validate formal-textbook-product-proposal` 與 `git diff --check -- docs/產品化企劃書_2026-09-05.md` 通過作為完成證據。

## 4. 被動、免回填的預設路徑

變更紀錄：2026-09-22 使用者要求任務以被動方式進行，且不依其回填才能前進。任務 3.1 的個人基線保留為可選診斷，不再是核心題解鎖條件。

- [x] 4.1 [after: 3.1] 依 104–114 年主章節出題年度覆蓋，建立每科 2 章、每章「母題＋同型題＋變式題」的固定第一輪，共 12 章、36 題、24 個 60 分鐘時段，完成後固定接續 6 科 114 年計時模考；同步更新首頁、企劃書、候選表與診斷表，使未回填者可直接開始且不必等待個人化分析。驗證目標為 36 個 QID 全部唯一、皆為 `verified` 且有專屬題解，所有本地連結存在，並以 `spectra analyze formal-textbook-product-proposal --json`、`spectra validate formal-textbook-product-proposal` 與 `git diff --check` 通過作為完成證據。

## 5. 核心得分型題解建置

每一題只有在 canonical 與年度彙整頁一致具備「考場標準作答、得分點拆解、完整教學推導、獨立驗算、常見失分」，且來源轉錄、公式、稽核雜湊與工作台 bundle 通過測試後，才算完成；既有 `verified` 狀態本身不等於五區塊已建置。

- [x] 5.1 [after: 4.1] 完成時段 1–6 的六科母題：`EE-114-01-3`、`EE-113-02-1`、`EE-113-03-6`、`EE-112-04-5`、`EE-114-05-2`、`EE-113-06-4`。並依官方裁切修正 `EE-113-03-6` 題幹由錯誤的 `e^{-x-2y}` 與四小題，還原為 `e^{-x-y/2}` 與三小題；以六題五區塊稽核、408 項完整測試、HTML／JavaScript 語法檢查、484 題切片與連結檢查、`git diff --check` 通過作為完成證據。
- [x] 5.2 [after: 5.1] 完成時段 7–12 的 12 題同型／變式題：`EE-112-01-4`、`EE-111-01-3`、`EE-110-02-3`、`EE-104-02-3`、`EE-104-03-4`、`EE-107-03-5`、`EE-106-04-3`、`EE-111-04-3`、`EE-109-05-3`、`EE-105-05-2`、`EE-112-06-3`、`EE-114-06-3`。並修正 `EE-104-03-4` 遺留的矛盾期望值與題解 LaTeX 分隔符；以 18 題五區塊回歸測試、409 項完整測試、HTML／JavaScript 語法檢查、484 題切片與連結檢查、`git diff --check` 通過作為完成證據。
- [x] 5.3 [after: 5.2] 完成時段 13–18 的六科第二母題：`EE-112-01-2`、`EE-114-02-3`、`EE-113-03-3`、`EE-114-04-4`、`EE-114-05-5`、`EE-110-06-4`。並依官方裁切修正 `EE-113-03-3` 題庫題幹由錯誤的 `1/(x^4+16)` 還原為 `sqrt(2)/(1+16x^4)`；以 24 題五區塊回歸測試、410 項完整測試、HTML／JavaScript 語法檢查、484 題切片與連結檢查、`git diff --check` 通過作為完成證據。
- [x] 5.4 [after: 5.3] 完成時段 19–24 的 12 題同型／變式題：`EE-111-01-2`、`EE-113-01-3`、`EE-112-02-3`、`EE-109-02-2`、`EE-111-03-5`、`EE-112-03-4`、`EE-113-04-2`、`EE-112-04-3`、`EE-112-05-4`、`EE-110-05-5`、`EE-104-06-3`、`EE-106-06-5`。固定路徑 36 題已全部具備五區塊，且 canonical、年度彙整頁、稽核雜湊與工作台 bundle 已同步；以 36 題結構回歸測試、410 項完整測試、HTML／JavaScript 語法檢查、484 題切片與連結檢查、`git diff --check` 通過作為完成證據。

## 6. 被動計時模考與錯因修復

- [x] 6.1 [after: 5.4] 建立 114 年六科固定模考執行包，使每科依序完成「120 分鐘閉卷模考＋30 分鐘得分證據核對＋30 分鐘單題修復」，錯因只需標在個人答案紙，不要求回填專案，也不因單科結果改變後續順序。完成證據為六科順序固定、29 個 QID 齊全、每科配分合計 100、六份官方原卷與六份完整題解入口可用、29 題皆為 `verified`，並以 `tests/test_passive_mock_pack.py`、完整測試、`spectra analyze formal-textbook-product-proposal --json`、`spectra validate formal-textbook-product-proposal` 與 `git diff --check` 通過作為驗證目標。
- [x] 6.2 [after: 6.1] 建立 108 年六科固定盲測執行包，以不同年度、相同 180 分鐘流程檢查方法能否移轉，不要求回填專案。選用 108 年是因其為目前除 114 年外六科題解全數 `verified` 的完整年度，且 30 題與核心路徑零重疊；答案紙上的 `○配分／×配分／T配分` 只描述本次作答證據，不以跨年度原始總分差宣稱改善。完成證據為六科順序固定、30 個 QID 齊全、每科配分合計 100、六份官方原卷與六份完整題解入口可用、30 題皆為 `verified`，並以 `tests/test_passive_retest_pack.py`、完整測試、`spectra analyze formal-textbook-product-proposal --json`、`spectra validate formal-textbook-product-proposal` 與 `git diff --check` 通過作為驗證目標。
- [x] 6.3 [after: 6.2] 將 104–114 年 66 份年度入口改由官方 PDF 卷首擷取試題代號與計算器規範，逐科呈現 canonical 稽核狀態與完整題解，不再把「題解已建置」誤寫成「使用者已掌握」，並把舊進度儀表板降為不阻擋固定路徑的可選紀錄。完成證據為 `scripts/build_year_readmes.py --check` 可重現 11 份年度頁、114／108 共 12 個官方原卷與題解入口齊全、108 年三科禁用計算器與六科試題代號符合官方卷首、產生範本不含虛構日期／分數，並以 `tests/test_year_readmes.py`、完整測試、`git diff --check` 通過作為驗證目標。
- [x] 6.4 [after: 6.3] 依 2026-09-22 查核之現行《專門職業及技術人員高等考試技師考試規則》第 10 條，修正高頻考點文件的及格敘述：保留一般全科 6 科與平均 60 分的原則，同時明列任一科 0 分、缺考計 0 分，以及未達全程到考人數 16% 時的排名／至少 50 分替代規則；把平均 70 分改稱內部備考緩衝，不再宣稱高頻題可保證命中或及格。完成證據為官方法規連結、323 題現況數字、完整限制條件皆出現在策略文件，且 `tests/test_year_readmes.py`、完整測試、Spectra 分析／驗證與 `git diff --check` 通過。
- [x] 6.5 [after: 6.4] 建立 114／108 年共 59 題的被動錯因修復索引，使每題可直接開啟 canonical 題解、看到既有 chapter 或 approved taxonomy 名稱，並依題型取得固定第一修復動作；15 題 canonical 缺 chapter 時明示 taxonomy 來源，不自行造題型，也不要求使用者回填。完成證據為 59 個唯一 QID、59 個本地 canonical 連結、59 題 `verified` 狀態與七種固定錯因規則皆由 `tests/test_passive_repair_index.py` 鎖定，並以完整測試、Spectra 分析／驗證及 `git diff --check` 通過作為驗證目標。
- [x] 6.6 [after: 6.5] 將舊有人工填寫的高頻考點與一律 55 題分母，改為直接依 323 題資料庫及 `QUESTION_TAXONOMY_MAP.primaryChapter` 重建的客觀章節覆蓋統計；每科先按出題年度數、再按題數排序，只用於決定複習先後，不再以「必考／送分／80-20」等字樣暗示未來命中或保證得分。首頁的舊 60／30／15 天自填流程同步改為 24 時段 → 混合橋接 → 114 模考 → 108 複測 → 59 題修復的免回填預設順序。完成證據為六科實際題數 48／48／67／55／50／55、每科前 5 章可重現、首頁與統計頁的被動路徑連結可用，並以 `scripts/analyze_topic_frequency.py --check`、`tests/test_topic_frequency_report.py`、完整測試、Spectra 分析／驗證及 `git diff --check` 通過作為驗證目標。
- [x] 6.7 [after: 6.6] 將 114 年模考與 108 年複測的 59 題全部補成真正可閱卷的五區塊得分型解答，並以題級 canonical 為唯一來源決定性重建十二份年度彙整頁；補強時依官方裁切修正電路超節點、電子學差動對參數、傅立葉係數與標準差等既有答案錯誤，對工業配電電弧爐模型等題意分支明列條件，不把不同模型混成唯一答案。完成證據為 59 題的「考場標準作答／得分點拆解／完整教學推導／獨立驗算／常見失分」各出現一次且內容非模板、十二份年度頁逐段與 canonical 一致，並以 `scripts/sync_mock_score_solutions.py --check`、`scripts/audit_mock_score_solutions.py`、`tests/test_mock_score_solution_audit.py`、完整測試、題庫 bundle 重建、Spectra 分析／驗證及 `git diff --check` 通過作為驗證目標。
- [x] 6.8 [after: 6.7] 將全題庫仍為 `needs_manual_review` 的 7 題從「精確解答」缺口改造為條件作答邊界：每題列出題幹缺口、可唯一得分的部分、假設先行的條件解，不把缺參數、多根或主值慣例冒充唯一答案；同時銷定 24 時段、18 題母題、114 模考與 108 複測只能引用 `verified` 題解。完成證據為 `docs/上榜精確解答邊界_條件題處理.md`、`scripts/audit_passive_route_status.py`、`tests/test_passive_route_status.py`、完整測試、Spectra 分析／驗證及 `git diff --check` 通過。
- [x] 6.9 [after: 6.8] 在 24 個核心時段與 114 年整卷之間補上六科 12 題混合橋接：每科重做兩個核心章節的變式題，題目入口不顯示章節名，以 50 分鐘閉卷、20 分鐘得分證據核對、20 分鐘重寫的固定節奏檢查能否自主辨識方法；不增加功能、不要求回填，也不依成績改變六科順序。完成證據為 12 題皆 `verified`、每題具五區塊得分型題解、原題圖與核對連結可用，並以 `tests/test_mixed_bridge_pack.py`、`scripts/audit_passive_route_status.py`、完整測試、Spectra 分析／驗證及 `git diff --check` 通過。
- [x] 6.10 [after: 6.9] 依考選部公告的 115 年技師考期，將既有「24 核心 → 混合橋接 → 114 模考 → 108 複測」固定路線日期化為 2026-09-23 至 11-13 的八段節奏，合計 69 小時，11-14 首日應試；不新增功能、不詢問每日可用時間，也不要求日期、分數或錯因回填。完成證據為官方考期與報名截止日可追溯、四段時數合計可重算、八個日期區段及落後處理規則完整、本地入口可用，並以 `tests/test_exam_countdown_plan.py`、完整測試、Spectra 分析／驗證及 `git diff --check` 通過。
- [x] 6.11 [after: 6.10] 將 24 個核心時段的 36 題入口改為「官方題目裁切圖」與「canonical 題解核對」兩欄，使用者可先開原題、停筆後才開答案，不必到工作台搜尋 QID，也降低誤看詳解風險。完成證據為 36 個 QID 各有且只有一個題目入口與一個核對入口、題目連結皆為存在的裁切圖、核對連結皆為對應 canonical，並以 `tests/test_score_oriented_core_path.py`、完整測試、Spectra 分析／驗證及 `git diff --check` 通過。
- [x] 6.12 [after: 6.11] 修正整卷量測污染：明示 114 年 29 題中有 6 題與核心路徑重疊，只定位為計時演練；108 年 30 題與核心路徑及混合橋接零重疊，定位為第一個盲測移轉點。不同年度難度不可比，不再以兩卷原始配分差宣稱進步。完成證據為重疊集合由測試重算並精確等於 6 題、108 年交集為空、首頁／倒數路線／企劃用語一致，並以 `tests/test_passive_mock_pack.py`、`tests/test_passive_retest_pack.py`、完整測試、Spectra 分析／驗證及 `git diff --check` 通過。
- [x] 6.13 [after: 6.12] 從每科兩題已驗證核心母題抽出六科考場得分骨架，統一為「定義 → 起手式 → 中間量 → 結論 → 快驗」，並補上各科不可省略的方向／工作區／定理條件／功率路徑／標么基準／設備額定證據；骨架不是官方配分表、不捏造分數，也不增加回填。完成證據為六科各兩個 `verified` 範本、13 個本地入口可用、核心／混合／114／108／倒數路線皆引用同一骨架，並以 `tests/test_exam_answer_skeleton.py`、`scripts/audit_passive_route_status.py`、完整測試、Spectra 分析／驗證及 `git diff --check` 通過。
- [x] 6.14 [after: 6.13] 依 2026-09-23 查核之考選部現行電機工程技師命題大綱，將 12 個核心章節逐項對照官方明列領域，確認全數仍在範圍內；另明列六科未被第一輪核心章節覆蓋的官方領域，避免把歷屆高頻誤稱完整大綱或命中保證。完成證據為官方目錄與 PDF 來源、六科 12 個 `verified` 母題映射、六列非核心範圍提醒，以及 `tests/test_official_outline_alignment.py`、`scripts/audit_passive_route_status.py`、完整測試、Spectra 分析／驗證及 `git diff --check` 通過。
- [x] 6.15 [after: 6.14] 將 59 題修復索引中 11 題 `S` 與 4 題 `T` 收斂為 15 張起手式急救卡，每張只揭露 canonical 的第一個可得分動作，不顯示最後答案；使用者卡住時不必打開整份詳解，也不需回填。同步修正 114／108 路線內五份電路 canonical 的裸 `qquad` 排版缺陷並決定性重建年度彙整頁。完成證據為 15 個 QID 與修復索引的 `S／T` 集合完全一致、15 個 canonical 連結可用、卡片不含 `\\boxed`、涉及題解不含裸 `qquad`，並以 `tests/test_first_move_rescue_cards.py`、`scripts/sync_mock_score_solutions.py --check`、`scripts/audit_mock_score_solutions.py`、完整測試、Spectra 分析／驗證及 `git diff --check` 通過。
- [x] 6.16 [after: 6.15] 將八段日期路線展開為 2026-09-23 至 11-13 的 52 天逐日開工表，每一天只指定一個固定任務組、恢復日或考前停止日；24 個核心時段、六科混合橋接、六科 114 演練與六科 108 盲測各出現一次，總時數仍為 69 小時，不要求使用者排課或回填。完成證據為 52 個連續日期無缺漏、42 個工作代碼無重複、11/12 與 11/13 不開新題、所有本地入口可用，並以 `tests/test_daily_start_plan.py`、`tests/test_exam_countdown_plan.py`、完整測試、Spectra 分析／驗證及 `git diff --check` 通過。
- [x] 6.17 [after: 6.16] 對 93 個主路線 QID 的 canonical 題解新增數學排版品質閘門，排除 fenced code 後阻擋裸 `quad／qquad／mathrm／text／frac／sqrt／begin／end` 等會直接顯示成殘字的 LaTeX 命令；據此修正 `EE-108-01-2`、`EE-108-06-4`、`EE-113-03-3` 與 `EE-113-03-6`，並同步年度彙整頁、題解雜湊與離線 bundle。完成證據為 `scripts/audit_route_math_markup.py` 報告 93／93 clean、`tests/test_route_math_markup.py`、年度／canonical 一致性、完整測試、Spectra 分析／驗證及 `git diff --check` 通過。
- [x] 6.18 [after: 6.17] 為六科整卷建立不依回填的 120 分鐘得分節奏：5 分鐘掃卷、90 分鐘第一輪、17 分鐘搶分、8 分鐘收尾；第一輪以 0.9 分鐘／配分產生 10／15／20／25／30 分題的 9／13.5／18／22.5／27 分鐘時間帽，並與 8 分鐘無進展停損並用。114／108 各科依實際配分明列可直接套用的第一輪配置，且同步修正六科骨架中的 `Y_bus` tab 殘字與三處裸括號公式。完成證據為兩年所有卷型第一輪皆重算為 90 分鐘、總時間重算為 120 分鐘、三份入口互相連結，並以 `tests/test_exam_pacing_card.py`、`tests/test_exam_answer_skeleton.py`、完整測試、Spectra 分析／驗證及 `git diff --check` 通過。
- [x] 6.19 [after: 6.18] 對逐日路線首題 `EE-114-01-3` 重新核對官方裁切圖與 canonical 中的 24 V、4 Ω、1 H、0.25 F、開關前 1 Ω 及電流方向；將標準解的驗算具體化為微分方程殘差與開關瞬間功率平衡 `115.2 = 92.16 + 23.04 W`，使使用者可自行重算而非只相信「代回成立」。同步 114 年年度題解、稽核雜湊與離線 bundle；以 59 題題級／年度對齊、完整測試、Spectra 分析／驗證及 `git diff --check` 通過作為完成證據。
- [x] 6.20 [after: 6.19] 修正整流組母題代表性：官方裁切顯示舊母題是直流二極體狀態題，不能教會週期整流；新路線依序採 `EE-110-02-3` 半波純電阻母題、`EE-106-02-4` 半波含續流二極體同型題、`EE-104-02-3` 三相六脈波變式題。將 106 年題解補成五個得分型區塊並以波形積分獨立驗算，更新候選表、大綱對照、考場骨架與企劃；核心總量仍為 36 題、24 時段、69 小時。完成證據為三題各有官方裁切與 `verified` 題解，題級／年度一致、93 題主路線全為 `verified`、完整測試、Spectra 分析／驗證與 `git diff --check` 通過。
- [x] 6.21 [after: 6.20] 將工業配電第二核心組從分類上混用的「閃爍 → 諧波共振」修正為同一機制的遞進：`EE-110-06-4` 電弧爐閃爍母題、`EE-104-06-3` 供電條件同型題、`EE-113-06-3` 串聯電抗器改善變式題。113 年題解補齊五個得分型區塊，混合橋接同步換題；106 年諧波共振仍保留於完整題庫。完成證據為 36 題核心與 12 題橋接唯一性、93 題主路線皆 `verified`、年度／canonical 題解一致、完整測試、Spectra 分析／驗證與 `git diff --check` 通過。
- [x] 6.22 [after: 6.21] 重新核對 110 年工業配電官方題圖的 A／B 點，修正附帶 B 點分支漏計爐用變壓器電抗的錯誤；主答案 A 點保持不變，歷史 19.05%／6.07 pu 明確標為圖上未標示的中間點，不冒稱 B 點。另對 RLC 頻域與機率離散抽樣兩個跨方法變式註明第一步，不增加題量或回填。完成證據為年度／canonical／離線 bundle 同步、題解回歸測試與 475 項完整測試、Spectra 分析／驗證及 `git diff --check` 通過。
- [x] 6.23 [after: 6.22] 修正工業配電高頻統計的章節名稱：原「非線性負載諧波分析與抑制」其實合併了 4 題電弧爐電壓閃爍／變動與 3 題諧波／共振，故改稱廣義「電力品質：電壓閃爍與諧波分析」，不把 7 題／6 年誤說成純諧波覆蓋。保留既有 24 時段與題號，更新分類名稱、知識節點、統計頁及工作台生成資料；以題號集合防止 4／3 計數漂移，並以 476 項完整測試、Spectra 分析／驗證及 `git diff --check` 通過作為完成證據。
- [x] 6.24 [after: 6.23] 將廣義電力品質節點的公式與提醒同步改成「先分辨閃爍或諧波，再選分壓／複數阻抗或頻率相關阻抗」，並移除「串聯電抗器可消除 5 次以上／3 次諧波」的過度保證；離線知識圖、Obsidian 筆記及工作台由來源重建。完成證據為節點回歸測試、知識圖 484 題映射、完整測試、Spectra 分析／驗證與 `git diff --check` 通過。
- [x] 6.25 [after: 6.24] 依 107 年工程數學第 5 題官方裁切，將主路線離散抽樣變式的標準作答聚焦於任意 `x=0,1,2,3` 的兩個完整分布；原題未單問 `x=1`，因此只保留在獨立驗算，不再列作必拿得分點。canonical 與年度題解、工程數學稽核索引及離線 bundle 同步，並以題解回歸測試、完整測試、Spectra 分析／驗證及 `git diff --check` 通過作為完成證據。
- [x] 6.26 [after: 6.25] 核對工程數學聯合密度母題與同型題的官方裁切：113 年題的三個輸出和 104 年題的四個動差均無數值矛盾。將 104 年題的考場標準解收斂為「先求邊際密度、證明獨立、用一維積分與乘積得四答」，完整推導仍保留原雙重積分，獨立驗算改由聯合密度直接重算 `E[XY]` 及檢查變異數。年度題解、數學稽核索引與離線 bundle 同步；以題解回歸測試、完整測試、Spectra 分析／驗證及 `git diff --check` 通過作為完成證據。
- [x] 6.27 [after: 6.26] 依 111 年電路學第 3 題官方裁切核對串聯 `R_o` 後的並聯 `R`／串聯 LC 拓撲，確認既有帶拒轉移函數與截止頻率數值。為 15 分的頻寬／轉角頻率小題，在考場標準作答補上以通帶增益 0.4 為基準的半功率方程與兩根式，並以根的積與差核對數值；年度題解、稽核索引與離線 bundle 同步，以完整測試、Spectra 分析／驗證及 `git diff --check` 通過作為完成證據。
- [x] 6.28 [after: 6.27] 複核 106 年電子學第 4 題官方續流電路及 `L\to\infty` 假設，三項數值計算原已正確；補上題目明問的 `v_o`、`i_{D1}`、`i_{D2}` 共用時間軸波形圖，清楚區分負載電流連續與電源僅正半週供電。SVG 與 canonical／年度入口一致，稽核索引和離線 bundle 更新；以連結／SVG 回歸測試、完整測試、Spectra 分析／驗證及 `git diff --check` 通過作為完成證據。
- [x] 6.29 [after: 6.28] 依 110 年電子學第 3 題官方裁切複核半波純電阻母題，三項主答案與 0.7 V 壓降敏感度重算一致；在考場標準解明列電壓與電源／負載電流的半波式及負半週皆零的邊界，避免誤套至 106 年強電感續流變式。年度題解、稽核索引與離線 bundle 同步，並以核心路線回歸測試、完整測試、Spectra 分析／驗證及 `git diff --check` 通過作為完成證據。
- [x] 6.30 [after: 6.29] 依 104 年電子學第 3 題官方裁切複核 380 V 線電壓、30 Ω 純電阻與六個小題；考場標準解補上 60° 脈波的負載電流均方值積分，並以三相輪替對稱性說明二極體／相線分得負載均方值的 1/3／2/3，避免把脈動電流誤當定值。年度題解、稽核索引與離線 bundle 同步；以核心路線回歸測試、完整測試、Spectra 分析／驗證及 `git diff --check` 通過作為完成證據。
- [x] 6.31 [after: 6.30] 依 113 年工程數學第 6 題官方裁切複核聯合密度與三小題；在考場標準解明寫 `p_X(x)` 並與 `p_Y(y)` 相乘證明獨立後再拆混合動差，完整推導修正純文字括號造成的數學符號歧義。年度題解、稽核索引與離線 bundle 同步；以核心路線回歸測試、完整測試、Spectra 分析／驗證及 `git diff --check` 通過作為完成證據。
- [x] 6.32 [after: 6.31] 依 112 年電機機械第 5 題官方裁切複核並激電動機電路與 1200 rpm 磁化曲線；把題圖直接附在單題 canonical 題解，讓有效磁場電流 3.5 A 的約 200 V 讀值可自行核對，並以 5 A 無載基準與場弱化後轉速高於 1200 rpm 的方向檢查結果。年度題解、稽核索引與離線 bundle 同步；以題圖連結回歸測試、完整測試、Spectra 分析／驗證及 `git diff --check` 通過作為完成證據。
- [x] 6.33 [after: 6.32] 依 114 年電力系統第 2 題官方裁切核對兩匯流排潮流、PV 無效功率上限與一次牛頓疊代；在標準解明示由正弦方程選正常運轉低功角分支的條件，不將反正弦主值冒稱唯一數學根，再據此判斷 111.31 Mvar 超限。年度題解、稽核索引與離線 bundle 同步；以分支條件回歸測試、完整測試、Spectra 分析／驗證及 `git diff --check` 通過作為完成證據。
- [x] 6.34 [after: 6.33] 依 113 年工業配電第 4 題官方單線圖核對低壓 CB、故障點 F 與感應馬達相對位置；保留 F 點總短路容量 17.42 MVA／20.96 kA 作為考題保守選用值，同時明列左側 CB 接點對此故障實際只流過上游支路 15.42 MVA／18.55 kA，不把負載側馬達倒灌誤作穿越 CB 的電流。題圖、年度題解、稽核索引與離線 bundle 同步；以拓撲／數值回歸測試、完整測試、Spectra 分析／驗證及 `git diff --check` 通過作為完成證據。
- [x] 6.35 [after: 6.34] 依 112 年電路學第 4 題官方並聯 RLC 圖核對兩個電流源總注入為 -6 A；修正標準解與完整推導中誤寫為齊次式的控制方程，使二階式右端為 -1500，並明示先取 -6 A 定值特解後才解齊次特徵根。題圖、年度題解、稽核索引與離線 bundle 同步；以符號回代、方程式回歸測試、完整測試、Spectra 分析／驗證及 `git diff --check` 通過作為完成證據。
- [x] 6.36 [after: 6.35] 依 111 年電路學第 3 題官方裁切圖核對串聯 Ro 後的 R 與串聯 LC 並聯拓樸、輸出端與半功率截止點；將官方圖直接附在單題題解，移除年度解答仍錯用串聯帶通式的過時敘述，改為辨認舊誤式並說明現行兩入口一致。年度題解、稽核索引與離線 bundle 同步；以直接阻抗數值回代、題圖與過時敘述回歸測試、完整測試、Spectra 分析／驗證及 `git diff --check` 通過作為完成證據。
- [x] 6.37 [after: 6.36] 依 104 年工程數學第 4 題與 107 年第 5 題官方裁切核對四個動差及放回／不放回任意 x 機率；在 104 年考場標準解明列兩個邊際密度的積分與支持集外為零，107 年標準解明列 x 不在 0 至 3 時機率皆零，使獨立性與完整機率函數的得分證據可直接抄寫。年度題解、稽核索引與離線 bundle 同步；以核心路線回歸測試、完整測試、Spectra 分析／驗證及 `git diff --check` 通過作為完成證據。
- [x] 6.38 [after: 6.37] 依 106 年電機機械第 3 題官方裁切核對外接軸負載轉矩與非零無載電樞電流，修正舊解把 100 N·m 直接等同電磁轉矩的未聲明假設。主路徑明示空載損失轉矩近似不變，得到約 56.89 A／866.4 rpm；另保留 52.89 A／876.5 rpm 作為忽略旋轉損失、把 100 N·m 當電磁轉矩時的條件值，且說明題面未給損失隨速模型故無唯一精確工作點。核心路線標註條件，年度題解、稽核索引與離線 bundle 同步；以數值與條件回歸測試、完整測試、Spectra 分析／驗證及 `git diff --check` 通過作為完成證據。
- [x] 6.39 [after: 6.38] 修正 115 年逐日開工表的「今天是 9/23」過期入口；日期列改作前項準時完成時的建議節奏，首次開工固定從 CORE-01 開始，已有進度則接最早未完成代碼，不能因當前日期較晚推定使用者已完成並跳題。維持不依回填、69 小時總量與 11/13 停止新增練習的邊界；以逐日表回歸測試、完整測試、Spectra 分析／驗證及 `git diff --check` 通過作為完成證據。
- [x] 6.40 [after: 6.39] 修正時段 10 的內容風險：106 年並激馬達題因外接軸轉矩與非零無載電流之間缺旋轉損失隨速模型，從核心路線撤下、降為 needs_manual_review，加入第 8 張條件題安全作答卡與官方來源／覆核理由；以 107 年串激馬達官方題替換，該題明給 10 A／200 A 兩工作點及磁化曲線參考反電勢，題解整理為五個得分區塊並附官方題圖。保持核心 36 題、24 時段與模考零重疊原則；年度題解、稽核索引與離線 bundle 同步，並以路線狀態、五區塊、完整測試、Spectra 分析／驗證及 `git diff --check` 通過作為完成證據。
- [x] 6.41 [after: 6.40] 將 106 年條件題加入後的現況數字同步至 README、產品企劃與本提案：PE 323 題為 300 verified、15 reference_book_verified、8 needs_manual_review，並修正企劃中誤稱 24 個核心時段尚未備妥的過期敘述。新增由兩份 audit manifest 重算三份對外文件數字的回歸測試；491 項完整測試、Spectra 分析／驗證及 `git diff --check` 通過。
- [x] 6.42 [after: 6.41] 從官方裁切獨立重算時段 10 的 107 年串激馬達兩工作點，固定 50 V、0.05 Ω、1200 rpm 及 10 A／40 V、200 A／48 V 參考電勢，驗證題解列出的 1485／1000 rpm 與 3.183／76.394 N·m；同時確認 106 年條件題僅留於安全作答卡，未經其他主路線入口帶回。以核心題回歸測試、完整測試、Spectra 分析／驗證及 `git diff --check` 通過。
- [x] 6.43 [after: 6.42] 依 111 年電機機械第 3 題官方裁切圖修正變式題的條件引用：第（二）小題未單獨給場控電阻，標準解先以滿載 KVL 與磁化表答約 1.25 A；1.254 A 明示為沿用第（三）小題 200 V 無載／滿載條件後的插值近似，不再冒稱第（二）題唯一精確值。官方題圖、路線提醒、年度題解、稽核索引與離線 bundle 同步；以條件／數值回歸測試、完整測試、Spectra 分析／驗證及 `git diff --check` 通過。
- [x] 6.44 [after: 6.43] 依官方題圖修正電子學核心母題錯置與 114 年模考題波形：原 114-02-3 是開關 RL 暫態，控制波形前半週開路、後半週短路，題目未給零初值；以周期穩態條件重算約 -15.025 A／-14.975 A 的兩個邊界並保留非穩態初值限制。DC–DC 核心改為 112-02-3 反相 buck-boost CCM 母題、107-02-3 同拓撲 DCM 題、109-02-2 Boost 變式；107 題補齊五個得分區塊。114 整卷與核心重疊改為 5 題，廣義 DC–DC 統計節點 11 題／8 年明列真正轉換器為 10 題／7 年。年度題解、稽核索引與離線 bundle 同步；以題圖／數值回歸測試、完整測試、路線狀態、Spectra 分析／驗證及 `git diff --check` 通過。
- [x] 6.45 [after: 6.44] 再核官方 109-02-2 Boost 題：題面未給「臨界電容」的容許漣波判準，故既有以已知 200 µF 算漣波再倒推 200 µF 的結論屬循環計算。保留可唯一作答的五項數值，將 Cc 改成依容許漣波比而定的條件式並降為 needs_manual_review；核心時段 20 與混合橋接改用 106-02-5 同步非反相 buck-boost 變式，標準解保留 Q1 RMS 的電感漣波精確項。主路徑 94 題皆維持 verified，人工覆核增至 9 題，年度頁、索引、稽核 manifest 與離線 bundle 同步；以公式回歸測試、完整測試、路徑稽核、Spectra 分析／驗證與 `git diff --check` 通過為完成證據。
- [x] 6.46 [after: 6.45] 核對 113-02-3 官方題圖，確認輸入串聯開關與接地續流二極體構成 Buck，修正原題級解答把它誤判成 Boost 的全部五小題：兩狀態、Vo=DVs、BCM 峰值 2Vo/R、Lmin=(1-D)R/(2fs)、峰對峰輸出漣波比 1/(4RCfs)。沿用已有的 Buck 兩狀態圖，年度題目導覽、稽核雜湊與離線 bundle 同步；以拓撲／數值回代測試、完整測試、Spectra 驗證及 `git diff --check` 通過為完成證據。
- [x] 6.47 [after: 6.46] 核對相鄰的 113-02-4 全橋中心抽頭題：官方圖只標 Np:Ns，未指定 Ns 為整段次級或半繞組，原解將半繞組匝比的 2DNs/Np 當唯一答案。改以半繞組對一次側匝比 nh 為共同式 Vo/Vs=2Dnh，分列 Ns 兩種定義並明示對角開關的 D 時序假設；BCM 臨界電感保留在該時序下的條件式。題目降為 needs_manual_review，加入第 10 張安全作答卡，年度頁、稽核索引與離線 bundle 同步；以雙匝比／電感回代測試、完整測試、路線稽核、Spectra 驗證與 `git diff --check` 通過為完成證據。
- [x] 6.48 [after: 6.47] 依 114 年工程數學第 4 題官方裁切確認傅立葉係數正確，但舊標準解將級數寫成與分段週期函數處處相等；在奇數倍 π 的接點題面指定值為 π，級數卻收斂到左右極限平均 π/2。把考場標準式改為級數展開記號並明列端點例外，補係數及端點部分和回歸測試；113 年第 6 題聯合密度亦依官方題圖核對正規化、邊際平均及混合動差無誤。離線 bundle 同步；以完整測試、Spectra 驗證及 `git diff --check` 通過為完成證據。
- [x] 6.49 [after: 6.48] 核對 114 年電力系統第 4、5 題官方裁切：第 5 題搖擺方程之 10π/3 rad/s² 與 12 週期後 3620 rpm 回代正確；第 4 題故障阻抗／支路數值正確，但官方單線圖未指定線路電流參考方向。考場標準解先聲明 I31、I21 取流向故障點，再列反方向 I13、I12 的相反符號，避免把方向約定誤判成另一數值答案；補官方裁切圖與 KCL／方向回歸測試。年度題解、稽核雜湊及離線 bundle 同步，以完整測試、Spectra 驗證與 `git diff --check` 通過為完成證據。
- [x] 6.50 [after: 6.49] 完成 114 年電力系統五題官方裁切的橫向核對，獨立回算相量共軛與 10 A／90°、PV 無效功率上限 111.314 Mvar 與一次 Newton 步、550 MW 經濟調度 250／300 MW、故障支路 KCL、二極機 12 週期後 3620 rpm；數值未見新矛盾。為第 1、3、5 題補官方裁切圖，新增跨五題常數／得分結論回歸測試。年度頁、稽核與離線 bundle 同步；以完整測試、Spectra 驗證及 `git diff --check` 通過為完成證據。
- [x] 6.51 [after: 6.50] 依 114 年電機機械第 5 題官方提示校正轉矩符號：題面明給 T=+φ²(dℜ/dθ)/2，舊解及其回歸測試卻鎖定負號。瞬時轉矩與平均轉矩改為正號；3.4055 N·m 與 0.642 kW 的最大量值保持不變，並區分反向轉速時機械功率的符號。標準解聲明零直流磁通的週期穩態假設，另註明初始磁通未給時的積分常數及其不影響同步平均量值。補官方題圖、兩個轉向的數值積分回歸測試；年度頁、稽核雜湊與離線 bundle 同步，以完整測試、Spectra 驗證及 `git diff --check` 通過為完成證據。
- [x] 6.52 [after: 6.51] 依 114 年電機機械第 3 題官方圖 3(a)(b) 校正二相旋轉磁場：t1/t3/t5/t7 兩繞組電流均為 ±Im/√2，並非舊解的一軸峰值／另一軸零；按紙面 x 向右、y 向上與繞組進出紙面方向，合成向量相位為 45°／135°／225°／315°，等幅逆時針旋轉，輔助繞組首尾對調後反向。新增官方波形取樣回歸測試，並把本題與第 5 題從錯誤的戴維寧等效／凸極同步機分類分出「單相二相旋轉磁場」與「磁阻電動機位置轉矩」兩章；同步章節詞彙、taxonomy、題解、修復索引、知識圖譜與工作台。以分類契約、完整測試、Spectra 驗證及 `git diff --check` 通過為完成證據。
